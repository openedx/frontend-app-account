import { SlotOperation } from '@openedx/frontend-base';

// This app contributes nothing to other apps' slots.  The slots it offers live in `./slots/`.
const slots: SlotOperation[] = [];

export default slots;
