// This import stays relative: frontend-base's webpack fallback plugin matches the literal
// `./messages` request to stub out the messages that translations:pull has not generated yet.
export { default } from './messages';
